//Activate dropdown & tab
$(".ui.pssdropdown").dropdown();
$(".ui.dropdown").dropdown();
$(".menu .item").tab();

// Global Variable
const NonceValue = "rdsystem2020";
const ColorOwner = "#00139f";
var vusrnm = getCookie("usrnm");
var vtoken = getCookie("token");
var restlocvideonum = decodeURIComponent(getCookie("restlocvideonum"));
let Ucrypt = new Encryption();
var Udecrypt = Ucrypt.decrypt(EncU, NonceValue);
const LogoutProtect = false;

function TableListFunc(FSearch, FYear, FNama, FBagian) {
  $("#DRRList").DataTable({
    dom:
      "<'ui stackable grid'" +
      "<'row'" +
      "<'left aligned sixteen wide column'>" +
      ">" +
      "<'row dt-table'" +
      "<'sixteen wide column'tr>" +
      ">" +
      "<'row'" +
      "<'left aligned eight wide column'B>" +
      "<'right aligned eight wide column'p>" +
      ">" +
      ">",
    buttons: [
      {
        text: "10",
        action: function (e, dt, node, config) {
          $("#DRRList").DataTable().page.len(10).draw();
        },
      },
      {
        text: "25",
        action: function (e, dt, node, config) {
          $("#DRRList").DataTable().page.len(25).draw();
        },
      },
      {
        text: "50",
        action: function (e, dt, node, config) {
          $("#DRRList").DataTable().page.len(50).draw();
        },
      },
      {
        text: "100",
        action: function (e, dt, node, config) {
          $("#DRRList").DataTable().page.len(100).draw();
        },
      },
      {
        text: "1000",
        action: function (e, dt, node, config) {
          $("#DRRList").DataTable().page.len(1000).draw();
        },
      },
    ],
    destroy: true,
    stateSave: true,
    processing: true,
    deferRender: true,
    serverSide: true,
    orderClasses: false,
    pageLength: 10,
    data: [],
    language: {
      sLengthMenu: "_MENU_",
      search: "",
      searchPlaceholder: "Search..",
      processing: '<i class="circle notch loading icon huge"></i>',
    },
    order: [[0, "desc"]],
    columns: [
      { data: null,
        render: function (data, type, row) {
            return parseInt(data[0]).toString(16).toUpperCase();
        },
      },
      { data: null,
        render: function (data, type, row) {
            return data[2];
        },
      },
      { data: null,
        render: function (data, type, row) {
          if (data[12] !=="" ) {
            return `<i class="file outline red viewdoc icon" id="${data[0]}" num="${data[4]}" rev="${data[5]}" ext="${data[12]}">`;
          } else {
            return `<i class="file outline greyish icon">`;
          }
        },
      },
      { data: 4 },
      { data: 5 },
      { data: 8 },
      { data: 7 },
      { data: 6 },
      { data: 10 },
      { data: null,
        render: function (data, type, row) {
          if ( data[10] == Udecrypt ) {
            return `<i class="edit outline editdrr iconcoloredit icon" id="${data[0]}" num="${data[4]}" chs="${data[8]}" typ="${data[7]}" des="${data[6]}" rev="${data[5]}">`;
          } else {
            return "";
          }
        },
      },
    ],
    columnDefs: [
      { className: "center aligned", targets: [0,1,2,4,5] },
      { className: "collapsing", targets: [0,1,3] },
      { orderable: false, targets: [2,9] },
    ],

    ajax: {
      url: `${restlocvideonum}table_drr`,
      type: "POST",
      headers: {
        user: vusrnm,
        token: vtoken,
      },
      data: {
        FSearch: FSearch,
        FYear: FYear,
        FNama: FNama,
      },
      error: function () {
        // window.location.href = "./?link=loclogout";
        Swal.fire(
          "Error",
          "Server tidak merespon, segera hubungi Administrator di ext. 3553 !",
          "error"
        );
      },
    },
  });

}


$(document).ready(function () {

  $("#addnewdrr").click(function(){
    $(".additem").modal('show');
    $('#inputchs').val('')
    $('#inputtype').val('')
    $('#inputdesc').val('')
  });

  $("#uploaddrr").click(function(){
    var id   = $('#einputid').val();
    var num  = $('#einputnum').val();
    $("#uinputid").val(id);
    $("#uinputnum").val(num);
    $("#udrrnumber").text(num);
    $(".uploaditem").modal('show');
    $(".edititem").modal('hide');
  });

  $("#DRRList").on("click", ".editdrr", function () {
    var id  = $(this).attr("id");
    var num = $(this).attr("num");
    var chs = $(this).attr("chs");
    var typ = $(this).attr("typ");
    var des = $(this).attr("des");
    var rev = $(this).attr("rev");
    $("#drrnumber").text(num);
    $("#einputid").val(id);
    $("#einputchs").val(chs);
    $("#einputtype").val(typ);
    $("#einputdesc").val(des);
    $("#einputnum").val(num);
    if(rev){
      $(".diveditrev").hide();
      setTimeout(function() { $('#einputdesc').focus() }, 1000);
    } else {
      $(".diveditrev").show();
    }
    $(".edititem").modal('show');
  });


  $("#DRRList").on("click", ".viewdoc", function () {
    var num = $(this).attr("num").replaceAll("/", ".");
    var rev = $(this).attr("rev");
    var ext = $(this).attr("ext");
    window.open("../RND_Upload/VideoDoc/DRR_"+num+rev+"."+ext, "_blank");
  });

  $("#tabtmpb").click(function(){
    window.location.href = "./?link=vidindex";
  });
  $("#tabta").click(function(){
    window.location.href = "./?link=vidindex&sub=TA";
  });
  $("#tabsercom").click(function(){
    window.location.href = "./?link=vidindex&sub=SC";
  });
  $("#tabcpv").click(function(){
    window.location.href = "./?link=vidindex&sub=CPV";
  });
  $("#tabdrr").click(function(){
    window.location.href = "./?link=vidindex&sub=DRR";
  });
  $("#tabfamily").click(function(){
    window.location.href = "./?link=vidindex&sub=FAM";
  });
  $("#tabsetting").click(function(){
    window.location.href = "./?link=vidindex&sub=SET";
  });


});

//SAVE NEW DRR
$('#savedata').on('click', function(e){
  var ichss = $('#inputchs').val();
  var itype = $('#inputtype').val();
  var idesc = $('#inputdesc').val();
  $.ajax({
    type: "POST",
    url: `${restlocvideonum}create_drr`,
    headers : {
      user  : vusrnm,
      token : vtoken
    },
    data: {
      ichss     : ichss,
      itype     : itype,
      idesc     : idesc,
    },
    cache: false,
    success: function (response){
      var obj = JSON.parse(response);
      if(isEmpty(obj)) {
        console.log('Gagal Simpan mas brooo, data kosong');
      } else if(obj) {
        if (obj.status == false){
          Swal.fire({
            text: obj.message,
            timer: 1500,
            icon: 'warning',
            showConfirmButton: false
          })
        } else {
          Swal.fire({
            title: 'Berhasil',
            text: obj.message,
            timer: 1500,
            position: 'top-end',
            icon: 'success',
            showConfirmButton: false
          })
        }
        $(".additem").modal('hide');
        var d = new Date();
        var yearnow = d.getFullYear();
        var json_str = getCookie('FilterDRR');
        var arr = JSON.parse(json_str);
        if(json_str){
          var sfilter = arr[0];
          var tfilter = arr[1];
          var mfilter = arr[2];
        } else {
          var sfilter = "";
          var tfilter = yearnow;
          var mfilter = "ALL";
        }
        TableListFunc( sfilter, tfilter, mfilter);

      } else {
        Swal.fire('Error','Ada kendal di koneksi/database, data tidak tersimpan !','error')
      }
    },
    error: function () {
      if (LogoutProtect){
        window.location.href = "./?link=loclogout";
      } else {
        Swal.fire("Error", "Server tidak merespon, segera hubungi Administrator di ext. 3553 !","error" );
      }
    },
  });
});


//SAVE EDIT DRR
$('#saveedit').on('click', function(e){
  var iid   = $('#einputid').val();
  var inum  = $('#einputnum').val();
  var ichss = $('#einputchs').val();
  var itype = $('#einputtype').val();
  var idesc = $('#einputdesc').val();
  $.ajax({
    type: "POST",
    url: `${restlocvideonum}update_drr`,
    headers : {
      user  : vusrnm,
      token : vtoken
    },
    data: {
      iid       : iid,
      ichss     : ichss,
      itype     : itype,
      idesc     : idesc,
      inum      : inum,
    },
    cache: false,
    success: function (response){
      var obj = JSON.parse(response);
      if(isEmpty(obj)) {
        console.log('Gagal Simpan mas brooo, data kosong');
      } else if(obj) {
        if (obj.status == false){
          Swal.fire({
            text: obj.message,
            timer: 1500,
            icon: 'warning',
            showConfirmButton: false
          })
        } else {
          Swal.fire({
            text: obj.message,
            timer: 1500,
            position: 'top-end',
            icon: 'success',
            showConfirmButton: false
          })
        }
        $(".edititem").modal('hide');
        var d = new Date();
        var yearnow = d.getFullYear();
        var json_str = getCookie('FilterDRR');
        var arr = JSON.parse(json_str);
        if(json_str){
          var sfilter = arr[0];
          var tfilter = arr[1];
          var mfilter = arr[2];
        } else {
          var sfilter = "";
          var tfilter = yearnow;
          var mfilter = "ALL";
        }
        TableListFunc( sfilter, tfilter, mfilter);

      } else {
        Swal.fire('Error','Ada kendal di koneksi/database, data tidak tersimpan !','error')
      }
    },
    error: function () {
      if (LogoutProtect){
        window.location.href = "./?link=loclogout";
      } else {
        Swal.fire("Error", "Server tidak merespon, segera hubungi Administrator di ext. 3553 !","error" );
      }
    },
  });
});



//REVISI DATA
$('#revisidata').on('click', function(e){

  var iid   = $('#einputid').val();
  var inum  = $('#einputnum').val();

  Swal.fire({
      title: '<h3>Revisi No '+inum+'?</h3>',
      icon: 'info',
      confirmButtonText: 'Ya',
      showCancelButton: true
    }).then((result) => {
      if (result.value) {
        $(".edititem").modal('hide');
        $.ajax({
          type: "POST",
          url: `${restlocvideonum}revisi_drr`,
          headers : {
            user  : vusrnm,
            token : vtoken
          },
          data: {
            inum  : inum,
            iid   : iid
          },
          cache: false,
          success: function (response){
            var obj = JSON.parse(response);
            if(isEmpty(obj)) {
              Swal.fire('Error','Server error, Data mungkin bermasalah !','error');
            } else if (obj.code == "error"){
              Swal.fire('',obj.message,'error');
            } else {
              Swal.fire('Berhasil','DRR berhasil direvisi.','success');
              var d = new Date();
              var yearnow = d.getFullYear();
              var json_str = getCookie('FilterDRR');
              var arr = JSON.parse(json_str);
              if(json_str){
                var sfilter = arr[0];
                var tfilter = arr[1];
                var mfilter = arr[2];
              } else {
                var sfilter = "";
                var tfilter = yearnow;
                var mfilter = "ALL";
              }
              TableListFunc( sfilter, tfilter, mfilter);
            }
          },
          error: function(){
            Swal.fire('Error','Server tidak merespon, segera hubungi Webmaster !','error')
          }
        });
      }
    })

});


// Upload DRR Dokumen
$("#saveupload").on("click", function () {
  var file_data = $("#uinputfile").prop("files")[0];
  var iddrr    = $('#uinputid').val();
  var numdrr   = $('#uinputnum').val();
  var form_data = new FormData();
  form_data.append("file", file_data);
  form_data.append("iddrr", iddrr);
  form_data.append("numdrr", numdrr);
  $.ajax({
    type: "POST",
    dataType: "text",
    contentType: false,
    processData: false,
    url: `${restlocvideonum}upload_drr`,
    headers: {
      user: vusrnm,
      token: vtoken,
    },
    data: form_data,
    cache: false,
    success: function (response) {
      var obj = JSON.parse(response);
      if (obj.status == false) {
        Swal.fire({
          text: obj.message,
          timer: 1500,
          icon: "warning",
          showConfirmButton: false,
        });
      } else {
        Swal.fire({
          text: "Uploaded !",
          timer: 2000,
          toast: true,
          showConfirmButton: false,
        });
        var d = new Date();
        var yearnow = d.getFullYear();
        var json_str = getCookie('FilterDRR');
        var arr = JSON.parse(json_str);
        if(json_str){
          var sfilter = arr[0];
          var tfilter = arr[1];
          var mfilter = arr[2];
        } else {
          var sfilter = "";
          var tfilter = yearnow;
          var mfilter = "ALL";
        }
        TableListFunc( sfilter, tfilter, mfilter);
        $(".uploaditem").modal("hide");
      }
    },
    error: function () {
      if (LogoutProtect){
        window.location.href = "./?link=loclogout";
      } else {
        Swal.fire("Error", "Server tidak merespon, segera hubungi Administrator di ext. 3553 !","error" );
      }
    },
  });
});


$(".canceldata").click(function(){
  $(".additem").modal('hide');
  $(".edititem").modal('hide');
  $(".uploaditem").modal('hide');
  $('#inputchs').val('')
  $('#inputtype').val('')
  $('#inputdesc').val('')
});



    
//Read Filter Cookies
var d = new Date();
var yearnow = d.getFullYear();

var json_str = getCookie('FilterDRR');
var arr = JSON.parse(json_str);
if(json_str){
  var sfilter = arr[0];
  var tfilter = arr[1];
  var mfilter = arr[2];
} else {
  var sfilter = "";
  var tfilter = yearnow;
  var mfilter = "ALL";
}

if (sfilter) {
  $("#fcari_drr").val(sfilter);
  var sfilterstart = sfilter;
} else {
  var sfilterstart = "";
}
if (tfilter) {
  $("#fthn_drr").dropdown("set selected", tfilter);
  var tfilterstart = tfilter;
} else {
  var tfilterstart = yearnow;
}
if (mfilter) {
  $("#fmn_drr").dropdown("set selected", mfilter);
  var mfilterstart = mfilter;
} else {
  var mfilterstart = "ALL";
}

var FiterArry = [sfilterstart, tfilterstart, mfilterstart];
var JsonFilter = JSON.stringify(FiterArry);
setCookie("FilterDRR", JsonFilter, 5);

//Load Table
TableListFunc( sfilterstart, tfilterstart, mfilterstart);

//RESPON KETIKA INPUT PENCARIAN DI KETIK
$("#fcari_drr").on("keyup", function () {
  var FilterCari = $("#fcari_drr").val();
  var FilterTahun = $("#fthn_drr").val();
  var FilterNama = $("#fmn_drr").val();
  
  var FiterArry = [FilterCari, FilterTahun, FilterNama];
  var JsonFilter = JSON.stringify(FiterArry);
  setCookie("FilterDRR", JsonFilter, 5);
  $("#PSSList").DataTable().destroy();
  TableListFunc(FilterCari, FilterTahun, FilterNama);
});

//RESPON KETIKA FILTER TAHUN DI RUBAH
$("#fthn_drr, #fmn_drr").on(
  "change",
  function () {
    var FilterCari = $("#fcari_drr").val();
    var FilterTahun = $("#fthn_drr").val();
    var FilterNama = $("#fmn_drr").val();
   
    var FiterArry = [FilterCari, FilterTahun, FilterNama];
    var JsonFilter = JSON.stringify(FiterArry);
    setCookie("FilterDRR", JsonFilter, 5);
    TableListFunc(FilterCari, FilterTahun, FilterNama);
  }
);

//RESET FILTER
$(".resetfilter").on("click", function () {
  var d = new Date();
  var yearnow = d.getFullYear();
  $("#fcari_drr").val("");
  $("#fthn_drr").dropdown("set selected", yearnow);
  $("#fmn_drr").dropdown("set selected", "ALL");
  var FiterArry = ["", yearnow, "ALL"];
  var JsonFilter = JSON.stringify(FiterArry);
  setCookie("FilterDRR", JsonFilter, 5);
  $("#PSSList").DataTable().destroy();
  TableListFunc("", yearnow, "ALL");
});
